[project]
name="northwind-sql-agent"
version= "0.1.0"
requires-python = ">=3.14"
dependencies = [
  "fastapi",
  "openrouter",
  "pydantic",
  "uvicorn"
]

[dependency-groups]
dev= ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
